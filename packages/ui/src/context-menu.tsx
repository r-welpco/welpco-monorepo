"use client";

import { ContextMenu as RadixContextMenu } from "@radix-ui/themes";
import { type ComponentPropsWithoutRef } from "react";

export const ContextMenu = RadixContextMenu.Root;
export const ContextMenuTrigger = RadixContextMenu.Trigger;
export const ContextMenuContent = RadixContextMenu.Content;
export const ContextMenuItem = RadixContextMenu.Item;
export const ContextMenuSeparator = RadixContextMenu.Separator;

